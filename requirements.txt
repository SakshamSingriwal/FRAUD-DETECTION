# ── Core
streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
imbalanced-learn>=0.11.0
joblib>=1.3.0

# ── Plotting
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Boosting (install all three or just what you need)
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0

# ── AutoML (optional — comment out if not needed)
# autogluon.tabular>=1.0.0   # pip install autogluon.tabular
# h2o>=3.44.0                # pip install h2o
# TPOT is not included by default due to dependency conflicts

# ── Utilities
tqdm>=4.65.0
